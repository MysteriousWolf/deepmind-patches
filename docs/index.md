# index.toml

Generated by `validate index` at release time from a validated checkout. Never
committed, never edited.

```toml
[catalogue]
schema  = 1                     # a reader refuses a higher number
version = "26.1.0"
commit  = "599439d2c54e738464123ee96edbd6d94960f333"
built   = "2026-09-21T07:40:00Z"
patches = 412

[taxonomy.genre]                # taxonomy.toml, verbatim
techno = "Four-to-the-floor, machine music."
# ...

[icons.categories]
Bass = [".......", ".......", "#......", "#.#....", "#.#.#..", "#.#.#.#", "......."]
# ...

[icons.badges]
arp = ["...#...", "..###..", ".#.#.#.", "...#...", ".#.#.#.", "..###..", "...#..."]
# ...

[icons.genre]                   # and icons.mood, icons.timbre, icons.role: one per term
techno = ["#.#.#.#", ".......", ".......", "#.#.#.#", "#.#.#.#", "#.#.#.#", "#.#.#.#"]
# ...

[palette.category]              # resources/palette.toml, verbatim
Bass = "#E4572E"
# ...

[[patch]]
id          = "bass/acid-growl-nyx"
name        = "Acid Growl"
author      = "nyx"
version     = 2
about       = "Resonant 303-ish bass that opens under velocity."
category    = "Bass"              # the folder name: "Sound Effects", not "SFX"
file        = "presets/Bass/Acid Growl - nyx.syx"
sha256      = "9f2b1c..."
fingerprint = "e3b0c4..."
licence     = "CC-BY-4.0"
genre       = ["techno", "industrial"]
mood        = ["dark", "aggressive"]
timbre      = ["gritty", "analogue"]
role        = ["bass"]
tags        = ["303"]
icon        = [".......", "..###..", ".#...#.", ".#...#.", ".#####.", ".#...#.", "......."]
own_icon    = true              # false when icon is the category fallback
created     = 2026-09-21
model       = "DeepMind 12"
firmware    = "1.1.5"

# Derived from the program bytes:
fx_mode     = "Insert"
effects     = ["Chorus", "Stereo Delay", "Room Reverb", "TC Deep Reverb"]
arp         = false
polyphony   = "Poly"
unison      = 1
routings    = 3
sequencer   = false
transpose   = 0

[[patch.demos]]
file    = "demos/Bass/Acid Growl - nyx.mp3"
about   = "Dry, C2 to C4."

[[patch.history]]               # one entry per version, oldest first
version     = 1
fingerprint = "a1b2c3..."

[[patch.history]]
version     = 2
fingerprint = "e3b0c4..."
```

`collection` appears when the patch sits in a collection folder. `arp_mode`
appears when the arpeggiator is on. Optional metadata fields appear only when
set.

The `deepmind-patches` crate reads this with `Index::parse`, looks up
fingerprints with `Index::lookup`, and builds pinned file URLs with
`Index::raw_url`.
